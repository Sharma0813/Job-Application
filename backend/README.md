# Job Portal Backend (Node/Express)

## Run
1. `cd backend`
2. `npm install`
3. `npm start`

The backend will run on port 3001 by default. It exposes:
- GET /api/jobs
- POST /api/apply
- GET /api/applications
